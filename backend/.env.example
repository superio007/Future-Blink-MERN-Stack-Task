# AI Flow Visualizer - Backend Environment Variables
# Copy this file to .env and update the values as needed

# Server Configuration
# Port number for the Express server
PORT=5000

# MongoDB Configuration
# Database connection string - choose one format below:

# Option 1: Local MongoDB installation
# Install MongoDB locally and use this connection string
MONGODB_URI=mongodb://localhost:27017/ai-flow-visualizer

# Option 2: MongoDB Atlas (Cloud)
# Create a free cluster at https://cloud.mongodb.com
# Replace <username>, <password>, and <cluster-url> with your values
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/ai-flow-visualizer?retryWrites=true&w=majority

# OpenRouter API Configuration
# Required for AI model access
# 1. Visit https://openrouter.ai/keys
# 2. Sign up for a free account
# 3. Generate an API key
# 4. Replace the placeholder below with your actual key
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Application Configuration
# URL of your frontend application (used for API attribution)
# Update this if deploying to a different domain
SITE_URL=http://localhost:3000

# Environment mode
# Options: development, production, test
NODE_ENV=development